apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "episilia-tail-fixed.fullname" . }}-test-connection"
  labels:
    {{- include "episilia-tail-fixed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "episilia-tail-fixed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
