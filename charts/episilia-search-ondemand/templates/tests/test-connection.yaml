apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "episilia-search-ondemand.name" . }}-test-connection"
  labels:
    {{- include "episilia-search-ondemand.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "episilia-search-ondemand.name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
