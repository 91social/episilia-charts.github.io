{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "episilia-search.serviceAccountName" . }}
  labels:
    {{- include "episilia-search.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.global.client.arn }}
    eks.amazonaws.com/role-arn: {{ .Values.global.client.arn }}
    {{- end }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}